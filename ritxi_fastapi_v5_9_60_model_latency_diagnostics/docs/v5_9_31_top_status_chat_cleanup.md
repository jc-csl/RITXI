# v5.9.31 · Chat limpio y estado superior

- `robot-summary-panel` se retira de la zona inferior.
- Se añade `robotTopline` en `top-status`.
- La entrada de mensaje pasa a 4 filas y más altura.
- Se deja visible solo `realtimeToggle` y `micStatus`.
- `resetBtn`, `unlockBtn` y `micBtn` quedan ocultos para no romper listeners existentes.
- El diagnóstico STT se oculta visualmente.
