# v5.9.7 · Corrección UI acciones/configuración

- Sin tarjeta `active` por defecto.
- Estado visual `En curso` y `Terminado` robusto.
- Bloqueo temporal de otras tarjetas mientras hay acción en ejecución.
- Cola visual si se pulsa otra acción.
- Modal real de configuración avanzada.
- Endpoints seguros:
  - `/api/config/files`
  - `/api/config/file?path=...`
