# Update 12.7.1 — Corrección de caché y tres columnas

La pantalla mostrada combinaba el HTML nuevo con CSS y JavaScript antiguos
almacenados por el navegador. Por eso aparecían títulos enormes, botones del
navegador, ausencia de fichas editables y secciones colocadas verticalmente.

La corrección incorpora:

- `panel_12_7_1.css`;
- `panel_12_7_1.js`;
- URLs versionadas;
- cabeceras HTTP `no-store`;
- metadatos HTML contra caché;
- diseño crítico de tres columnas dentro del HTML;
- scroll interno independiente por columna;
- versión visible `0.12.7.1`.
